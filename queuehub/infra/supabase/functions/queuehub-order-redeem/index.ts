const ALLOWED_ORIGIN='https://paq6809.github.io';const corsHeaders={'Access-Control-Allow-Origin':ALLOWED_ORIGIN,'Access-Control-Allow-Headers':'apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS','Vary':'Origin'};function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{...corsHeaders,'Content-Type':'application/json','Cache-Control':'no-store'}})}async function sha256Hex(value:string){const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return[...new Uint8Array(digest)].map(b=>b.toString(16).padStart(2,'0')).join('')}Deno.serve(async(req:Request)=>{const origin=req.headers.get('Origin');if(origin&&origin!==ALLOWED_ORIGIN)return json({ok:false,error:'origin_not_allowed'},403);if(req.method==='OPTIONS')return new Response(null,{status:204,headers:corsHeaders});if(req.method!=='POST')return json({ok:false,error:'method_not_allowed'},405);const supabaseUrl=Deno.env.get('SUPABASE_URL');const serviceRole=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY');if(!supabaseUrl||!serviceRole)return json({ok:false,error:'server_not_configured'},500);let body:Record<string,unknown>;try{body=await req.json()}catch{return json({ok:false,error:'invalid_request'},400)}const token=typeof body.token==='string'?body.token.trim():'';if(!/^[A-Za-z0-9_-]{43}$/.test(token))return json({ok:false,error:'invalid_or_expired'},400);const tokenHash=await sha256Hex(token);const upstream=await fetch(`${supabaseUrl}/rest/v1/rpc/queuehub_redeem_order_qr_service`,{method:'POST',headers:{apikey:serviceRole,Authorization:`Bearer ${serviceRole}`,'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify({p_token_hash:tokenHash})});const text=await upstream.text();let result:Record<string,unknown>|null=null;try{result=text?JSON.parse(text):null}catch{result=null}if(!upstream.ok)return json({ok:false,error:'temporarily_unavailable'},503);if(!result?.ok)return json({ok:false,error:'invalid_or_expired'},404);return json({ok:true,restaurantSlug:result.restaurant_slug,queueSessionId:result.queue_session_id,ticketNumber:result.ticket_number,expiresAt:result.expires_at})});
