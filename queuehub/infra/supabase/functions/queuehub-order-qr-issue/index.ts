const ALLOWED_ORIGIN='https://paq6809.github.io';const APP_BASE='https://paq6809.github.io/queuehub/';const corsHeaders={'Access-Control-Allow-Origin':ALLOWED_ORIGIN,'Access-Control-Allow-Headers':'authorization, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS','Vary':'Origin'};function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{...corsHeaders,'Content-Type':'application/json','Cache-Control':'no-store'}})}function decodeJwtSub(authHeader:string|null):string|null{if(!authHeader?.startsWith('Bearer '))return null;const part=authHeader.slice(7).split('.')[1];if(!part)return null;try{const n=part.replace(/-/g,'+').replace(/_/g,'/');const p=n+'='.repeat((4-n.length%4)%4);const payload=JSON.parse(atob(p));return typeof payload.sub==='string'?payload.sub:null}catch{return null}}function base64Url(bytes:Uint8Array){let binary='';for(const b of bytes)binary+=String.fromCharCode(b);return btoa(binary).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/g,'')}async function sha256Hex(value:string){const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return[...new Uint8Array(digest)].map(b=>b.toString(16).padStart(2,'0')).join('')}Deno.serve(async(req:Request)=>{const origin=req.headers.get('Origin');if(origin&&origin!==ALLOWED_ORIGIN)return json({error:'origin_not_allowed'},403);if(req.method==='OPTIONS')return new Response(null,{status:204,headers:corsHeaders});if(req.method!=='POST')return json({error:'method_not_allowed'},405);const actorUserId=decodeJwtSub(req.headers.get('Authorization'));if(!actorUserId)return json({error:'invalid_authenticated_user'},401);const supabaseUrl=Deno.env.get('SUPABASE_URL');const serviceRole=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY');if(!supabaseUrl||!serviceRole)return json({error:'server_not_configured'},500);let body:Record<string,unknown>;try{body=await req.json()}catch{return json({error:'invalid_json'},400)}const restaurantId=typeof body.restaurantId==='string'?body.restaurantId:'';const ticketNumber=Number(body.ticketNumber);const expiresInMinutes=Math.min(720,Math.max(15,Number(body.expiresInMinutes||480)));if(!restaurantId)return json({error:'restaurant_id_required'},400);if(!Number.isInteger(ticketNumber)||ticketNumber<=0)return json({error:'invalid_ticket_number'},400);if(!Number.isFinite(expiresInMinutes))return json({error:'invalid_expiry'},400);const raw=new Uint8Array(32);crypto.getRandomValues(raw);const token=base64Url(raw);const tokenHash=await sha256Hex(token);const expiresAt=new Date(Date.now()+expiresInMinutes*60000).toISOString();const upstream=await fetch(`${supabaseUrl}/rest/v1/rpc/queuehub_issue_order_qr_service`,{method:'POST',headers:{apikey:serviceRole,Authorization:`Bearer ${serviceRole}`,'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify({p_actor_user_id:actorUserId,p_restaurant_id:restaurantId,p_ticket_number:ticketNumber,p_token_hash:tokenHash,p_expires_at:expiresAt})});const text=await upstream.text();let result:unknown=null;try{result=text?JSON.parse(text):null}catch{result={raw:text}}if(!upstream.ok)return json({error:'issue_rejected',detail:result},upstream.status>=400&&upstream.status<500?upstream.status:500);const redeemUrl=`${APP_BASE}#/redeem?token=${encodeURIComponent(token)}`;return json({ok:true,token,redeemUrl,expiresAt,ticketNumber,result})});
