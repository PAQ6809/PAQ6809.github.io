const QueueHubAdminRemote={
  async invoke(body){const token=await QueueHubAdminAuth.ensureFresh();if(!token||!QueueHubAdminGuard.isLive())throw new Error('尚未取得 QueueHub 店員權限');const c=window.QueueHubRuntimeConfig.supabase;const res=await fetch(`${String(c.url).replace(/\/$/,'')}/functions/v1/queuehub-admin-command`,{method:'POST',headers:{apikey:c.publishableKey,Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});const text=await res.text();let data=null;try{data=text?JSON.parse(text):null}catch{data={message:text}}if(!res.ok)throw new Error(data?.detail?.message||data?.error||`正式指令失敗 ${res.status}`);return data},
  async queue(frontRestaurantId,action,number=null){const r=getRestaurant(frontRestaurantId);if(!r?.dbId)throw new Error('目前餐廳資料不是正式 Supabase snapshot');const data=await this.invoke({kind:'queue',restaurantId:r.dbId,action,number});await QueueHubProviders.refreshVenue({broadcast:true,notify:true,renderAfter:false});await QueueHubAdminData.refreshEvents(frontRestaurantId);render();return data},
  async integration(frontRestaurantId,cfg){const r=getRestaurant(frontRestaurantId);if(!r?.dbId)throw new Error('目前餐廳資料不是正式 Supabase snapshot');const data=await this.invoke({kind:'integration',restaurantId:r.dbId,type:cfg.type,enabled:cfg.enabled,apiEndpoint:cfg.apiEndpoint,webhookPath:cfg.webhookUrl,pollingIntervalSeconds:cfg.pollingIntervalSeconds,apiKeySecretName:cfg.apiKeySecretName,gatewayDeviceId:cfg.gatewayDeviceId});QueueHubVenueRepository.saveIntegration(frontRestaurantId,cfg);render();return data}
};
