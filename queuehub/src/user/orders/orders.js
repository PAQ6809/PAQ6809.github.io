window.trackOrder=(restaurantId,ticketNumber,options={})=>{const r=getRestaurant(restaurantId);if(!r)return false;const active=activeOrders();if(active.length>=10){toast('最多同時追蹤 10 張取餐單');return false}const queueSessionId=options.queueSessionId||currentQueueSession(r);const duplicate=active.find(o=>o.restaurantId===restaurantId&&o.queueSessionId===queueSessionId&&o.ticketNumber===ticketNumber);if(duplicate){if(!options.silent){toast('這張取餐單已經在追蹤中');go('/my-orders')}return false}const lead=Number(document.getElementById('lead')?.value||options.lead||3);state.orders.push({id:'ord_'+cryptoRandom(),visitorSessionId:state.visitor.id,restaurantId,queueSessionId,ticketNumber,orderToken:options.orderToken||undefined,notificationLead:[1,3,5,10].includes(lead)?lead:3,notificationsEnabled:state.visitor.notificationsEnabled,createdAt:new Date().toISOString(),completedAt:null});persist();toast(`${r.name} #${ticketNumber} 已加入我的取餐`);if(options.navigate!==false)go('/my-orders');return true}
window.completeOrder=id=>{const o=state.orders.find(x=>x.id===id);if(!o)return;o.completedAt=new Date().toISOString();persist();toast('已標記完成');render()};
window.removeOrder=id=>{const i=state.orders.findIndex(x=>x.id===id);if(i<0)return;state.orders.splice(i,1);persist();toast('已移除取餐單');render()};
function renderMyOrders(){const orders=activeOrders().map(o=>({o,r:getRestaurant(o.restaurantId),s:orderStatus(o)})).filter(x=>x.r).sort((a,b)=>a.s.rank-b.s.rank||a.s.diff-b.s.diff);if(!orders.length)return `<div class="pageHead"><div><h2>我的取餐</h2><div class="muted">同時追蹤多家餐廳，不用來回盯螢幕。</div></div></div><div class="panel empty"><strong>目前沒有進行中的取餐單</strong>到餐廳頁輸入取餐號碼，或直接掃收據 QR 即可加入。<div style="margin-top:14px"><button class="btn primary" onclick="go('/')">搜尋餐廳</button></div></div>`;const first=orders[0];return `<div class="pageHead"><div><h2>我的取餐</h2><div class="muted">${orders.length} 張進行中 · 系統依快到程度排序</div></div></div><div class="notice"><strong>建議先注意：${first.r.name} #${first.o.ticketNumber}</strong><br>${first.s.label}${first.s.estimate?`，估計約 ${first.s.estimate} 分鐘`:''}。</div>${!state.visitor.notificationsEnabled?`<div class="panel" style="padding:16px;margin-bottom:14px"><div class="row between"><div><strong>開啟快到提醒</strong><div class="muted" style="font-size:12px">頁面收到即時叫號更新時，可用系統通知把你帶回正確訂單。真正關閉瀏覽器後的背景推播需正式後端。</div></div><button class="btn primary" onclick="requestQueueNotifications()">開啟通知</button></div></div>`:''}<section class="orders">${orders.map(({o,r,s})=>`<article class="panel order"><div><div class="orderUrgency ${s.cls}">${s.label}</div><div class="orderTitle">${r.name}</div><div class="orderNums"><div>目前<b>${r.current}</b></div><div>我的號碼<b>${o.ticketNumber}</b></div><div>預估<b>${s.estimate?`${s.estimate}m`:'—'}</b></div></div><div class="muted" style="font-size:11px;margin-top:8px">提醒設定：提前 ${o.notificationLead} 組 · ${fmtAgo(r.updated)}更新</div></div><div class="orderRight"><div class="countdown">${s.diff>0?s.diff:0}<span style="font-size:11px;color:var(--muted)"> 組</span></div><div class="actions" style="justify-content:flex-end;margin-top:8px"><button class="btn" onclick="go('/restaurant/${r.id}?focusOrder=${o.id}')">查看</button><button class="btn" onclick="completeOrder('${o.id}')">已取餐</button><button class="btn ghost" onclick="removeOrder('${o.id}')">移除</button></div></div></article>`).join('')}</section><div class="panel" style="margin-top:14px;padding:18px"><div class="row between"><div><strong>離開網頁也不必重掃</strong><div class="muted" style="font-size:12px">目前裝置會記住這些訂單；再次開啟 QueueHub 時會自動恢復。</div></div><button class="btn primary" onclick="go('/')">再加入一張</button></div></div>`}
