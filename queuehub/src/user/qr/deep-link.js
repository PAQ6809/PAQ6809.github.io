let handledDeepLink='';
function handleDeepLink(){const base=routeBase();if(!base.startsWith('/restaurant/'))return;const key=path();if(handledDeepLink===key)return;const params=routeParams();const ticket=Number(params.get('ticket'));if(!Number.isFinite(ticket)||ticket<=0)return;const restaurantId=base.split('/')[2];const r=getRestaurant(restaurantId);if(!r)return;handledDeepLink=key;const queueSessionId=params.get('session')||currentQueueSession(r);const orderToken=params.get('token')||undefined;const added=window.trackOrder(restaurantId,ticket,{queueSessionId,orderToken,lead:3,navigate:false,silent:true});if(added){setTimeout(()=>go('/my-orders'),350)}else if(activeOrders().some(o=>o.restaurantId===restaurantId&&o.queueSessionId===queueSessionId&&o.ticketNumber===ticket)){setTimeout(()=>go('/my-orders'),250)}}
