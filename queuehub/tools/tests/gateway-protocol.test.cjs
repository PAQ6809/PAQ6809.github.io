const assert=require('node:assert/strict');
(async()=>{const p=await import('../gateway-agent/protocol.mjs');const id=p.generateIdentity();const body=JSON.stringify({kind:'queue',restaurantId:'00000000-0000-0000-0000-000000000001',action:'next',idempotencyKey:'gateway:test:0001'});const headers=p.signRequest(id.privateKeyPem,body,{deviceKey:id.deviceKey,keyVersion:1,timestamp:String(Date.now()),nonce:'abcdefghijklmnop'});assert.equal(p.verifyRequest(id.publicKeyPem,body,headers),true);assert.equal(p.verifyRequest(id.publicKeyPem,body+'x',headers),false);assert.match(headers['x-queuehub-device'],/^gw_/);assert.equal(headers['x-queuehub-key-version'],'1');console.log('QueueHub gateway protocol smoke: PASS')})().catch(e=>{console.error(e);process.exit(1)});
