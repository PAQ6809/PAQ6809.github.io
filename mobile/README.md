# Archived prototype

The former Capacitor experiment in this directory has been retired.

The only canonical iOS and Android application is now:

```text
reelscribe-mobile/
```

It uses React Native, whisper.rn/whisper.cpp, an app-owned iOS/Android native manager, Apple Vision OCR, Google ML Kit OCR, store preflight checks, model integrity controls and dedicated native build workflows.

Do not add new application code, dependencies, store metadata or CI workflows under `mobile/`. Existing historical documents may be removed after repository cleanup; they are not release inputs.
